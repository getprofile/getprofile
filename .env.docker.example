# =============================================================================
# GetProfile Docker Environment Configuration
# Copy this to .env for Docker deployment: cp .env.docker.example .env
#
# IMPORTANT: Only secrets and server settings belong here.
# All other configuration should go in getprofile.config.json
# See config/getprofile.example.json for configuration options.
# =============================================================================

# ============================================
# REQUIRED SECRETS
# ============================================

# LLM API Key (provider-agnostic)
# Works with OpenAI, Anthropic, or any OpenAI-compatible provider
LLM_API_KEY=sk-...

# OR use provider-specific keys (optional)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-...

# ============================================
# OPTIONAL SECRETS
# ============================================

# Proxy Authentication
# If set, clients must provide this as Bearer token in Authorization header
# If not set, proxy accepts all requests (useful for local development)
# GETPROFILE_API_KEY=your-secret-key-here

# ============================================
# SERVER SETTINGS
# ============================================

# Server port and host (defaults work for Docker)
# PORT=3100
# HOST=0.0.0.0

# ============================================
# DOCKER-MANAGED (DO NOT CHANGE)
# ============================================

# Database URL is automatically set by docker-compose.yml
# Do not override unless using external database
# DATABASE_URL=postgresql://getprofile:password@db:5432/getprofile

# =============================================================================
# DEPRECATED ENVIRONMENT VARIABLES
# The following variables are deprecated and should be moved to
# getprofile.config.json for better organization:
#
# - UPSTREAM_PROVIDER, UPSTREAM_BASE_URL (use config.upstream)
# - GETPROFILE_MAX_MESSAGES (use config.memory.maxMessagesPerProfile)
# - GETPROFILE_SUMMARY_INTERVAL (use config.memory.summarizationInterval)
# - GETPROFILE_RATE_LIMIT (use config.server.rateLimit)
#
# These variables are still supported for backward compatibility.
# =============================================================================
