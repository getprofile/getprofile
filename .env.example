# =============================================================================
# SECRETS AND HIGH-LEVEL CONFIG
# Only secrets and server settings belong here.
# All other configuration should go in getprofile.config.json
# =============================================================================

# Database Connection (secret)
DATABASE_URL=postgresql://getprofile:password@localhost:5432/getprofile

# LLM API Keys (secrets - provider-agnostic)
# Use LLM_API_KEY for the default provider, or provider-specific keys
LLM_API_KEY=sk-...                     # Generic API key (works with any provider)
# OPENAI_API_KEY=sk-...                # OpenAI-specific (optional, falls back to LLM_API_KEY)
# ANTHROPIC_API_KEY=sk-...             # Anthropic-specific (optional, falls back to LLM_API_KEY)

# Proxy Authentication (secret - optional)
# GETPROFILE_API_KEY=your-secret-key   # Require API key for proxy requests

# Server Settings
PORT=3100                              # Server port
HOST=0.0.0.0                           # Server host

# =============================================================================
# DEPRECATED (moved to getprofile.config.json)
# These variables are still supported for backward compatibility but should
# be moved to the config file:
# - UPSTREAM_PROVIDER, UPSTREAM_BASE_URL
# - GETPROFILE_MAX_MESSAGES, GETPROFILE_SUMMARY_INTERVAL, GETPROFILE_RATE_LIMIT
# =============================================================================
